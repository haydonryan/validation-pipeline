---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: pcfnorm/rootfs

inputs:
  - name: validation-pipelines

run:
  path: ./validation-pipelines/check-certs/task.sh

params:
  SYSTEM_DOMAIN:
  APPS_DOMAIN:
  POE_SSL_CERT1:
  POE_SSL_KEY1:
  TRUSTED_CERTS:
  