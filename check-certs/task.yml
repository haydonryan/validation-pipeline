---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: pcfnorm/rootfs

params:
  SYSTEM_DOMAIN:
  APPS_DOMAIN:
 
  POE_SSL_CERT1:
  POE_SSL_KEY1:
  TRUSTED_CERTS:
  
inputs:
  - name: validator-pipelines

run:
  path: pcf-pipelines/tasks/config-ert/task.sh